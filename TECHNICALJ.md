# AIradio 技術メモ

設計の理由、実測値、そこから決めたこと。手順は [READMEJ.md](READMEJ.md)。
English: [TECHNICAL.md](TECHNICAL.md)。

このファイルは旧 `HANDOFF.md` (設計仕様) / `BENCH.md` (Phase 0 ベンチ) /
`PROGRESS.md` (実装記録) を統合したもの (2026-08-11 に 3 ファイルは削除)。
ソースコードのコメントはこのファイルの節番号を参照している。

---

## 1. 全体の流れ

```
  Gcrawler/news/YYYY-MM-DD/*.json     ← RSS + スクショ + AI要約 (別プロジェクト)
          │  ファイル契約のみで疎結合
  ┌───────▼──────────────────────────────────────────┐
  │ news_service.py   番組進行の司令塔 (asyncio)      │
  │   ニュース選定 → Qwen3.6 で原稿 → VOICEVOX で合成 │
  └───────┬──────────────────────────┬───────────────┘
          │ program キューへ push     │ WebSocket
  ┌───────▼────────┐                 │  字幕・背景・リップシンク
  │   Liquidsoap   │  on_track ──────┤
  │  (1本のキュー) │  HTTP POST      │
  └───────┬────────┘                 │
          │                    ┌─────▼─────────┐
  ┌───────▼────────┐           │  web/ (VRM)   │
  │    Icecast     │           └───────────────┘
  │ :8100/radio.mp3│
  └────────────────┘
          ▲
  ┌───────┴────────┐
  │  bgm_worker.py │  HeartMuLa で BGM を常時生成 → cache/bgm_pool/
  └────────────────┘  (独立プロセス。落ちても番組は止まらない)
```

### プロセス構成

| プロセス | venv | 役割 |
|---|---|---|
| `news_service.py` | `AIradio/.venv` | 番組進行・原稿生成・TTS・表示系 HTTP/WebSocket (:8765) |
| `bgm_worker.py` | `~/heartlib/.venv` | HeartMuLa で BGM を生成しプールへ置く |
| llama-server | — | 原稿生成 (:8080) |
| VOICEVOX ENGINE | docker | TTS (:50021) |
| Liquidsoap | — | 積まれた wav を順に再生 (telnet :1234) |
| Icecast | — | 配信 (:8100) |

**`bgm_worker` だけ別 venv で動く。** torch (ROCm/gfx1151) が要るためで、
`AIradio/.venv` には torch を入れない (入れると news_service の起動が重くなり、
ROCm のロード順問題も持ち込むことになる)。両者の唯一の接点は
`scripts/bgm_pool.py` のファイル契約なので、このモジュールは
**標準ライブラリだけに依存させること**。

---

## 2. 確定している設計判断 (変更しないこと)

1. 全体制御は `news_service.py` (asyncio) が一元的に持つ。
   **Liquidsoap は積まれた順に再生するだけ**で、判断を持たない
2. BGM はニュースと**連動しない**独立生成。プールバッファ方式
3. 既定 BGM は Lo-Fi。プロンプトは `settings.toml` で差し替え、
   反映は**再起動運用** (ホットリロードは実装しない)
4. ニュース選定は直近 N 日分から除外リングを除いた一様ランダム
5. Gcrawler は別プロジェクト。**ファイル契約のみ**で連携する
6. 起動時は自己紹介 (LLM 生成 + 固定フォールバック)
7. フィラーは相槌 (事前生成) + ショートトーク (都度生成) のリピート
8. 生成に失敗しても番組は止めない (§9)

AIradio が Gcrawler に前提とするのは、`[news] crawler_dir` 配下に
`YYYY-MM-DD/*.json` と `*.png` が並んでいることだけ。

---

## 3. 番組ステートマシン

```
起動
 └─ INTRO         自己紹介 (LLM生成、ニュースは読まない)
      ↓
 ┌─ SPEAKING      ニュース原稿を TTS 再生 (約30秒)
 │    ↓ スピーチ終了時に BGM プールを確認
 │    ├─ プールに曲あり → MUSIC_PLAYING
 │    └─ プール空       → FILLER_LOOP
 │                         相槌 + ショートトークをリピート。
 │                         BGM が届いた時点で、再生中のフィラーは
 │                         自然に終わらせてから音楽へ
 │    ↓
 ├─ MUSIC_PLAYING BGM 再生 (30秒)。この間に裏で次ニュースの
 │                原稿生成 (LLM) + TTS 合成を済ませる
 └────↩ 音楽終了で SPEAKING に戻る
```

- **FILLER_LOOP は異常系ではない。** プールが空のときの正常な待ち方で、
  「生成のブレ・起動直後」のための保険。主経路ではない
- 起動直後の `want` は `news`。INTRO の次に曲が鳴らないよう、
  §3 の順序 (INTRO → SPEAKING → MUSIC) に合わせている
  (当初 `music` になっていて INTRO の次に曲が鳴っていた)

### INTRO

LLM 生成。プロンプトに番組名・DJ 設定・当日日付を渡す。
**LLM 失敗時の固定フォールバック文を必ず持つ。**

番組内で名乗る名前は `[dj] name`。**`[voicevox] speaker_name` とは別物**で、
あちらは (1) VOICEVOX の話者検索 (2) 規約で必要なクレジット表示 に使うので、
実際に合成した話者名から変えてはいけない。`[dj] name` が空なら speaker_name を
そのまま名乗る (後方互換)。

### FILLER_LOOP

- 固定相槌 (「えーと」「そうですね」等 8 種) は**起動時までに事前生成**して
  `cache/fillers/` にプールする。突入した瞬間に鳴らせないと、そこが無音になる
- ショートトークは LLM + TTS で都度生成。突入と同時に 1 本目の生成を始め、
  相槌でつなぐ
- **ショートトーク 1 本は 5〜10 秒に抑える** (`[script] filler_max_chars`)。
  長いと BGM への切り替え反応が鈍る
- **フィラーの末尾には無音を入れる** (`[script] filler_gap_sec`、既定 0.8 秒)。
  トークの生成が消費に追いつかないと相槌にフォールバックし続けるので、間が
  無いと 1 秒間隔でまくし立てる感じになる。VOICEVOX の `postPhonemeLength` を
  上書きして**合成の時点で焼き込む**。ffmpeg で後処理しないのは、`duration_sec`
  と viseme がそのまま辻褄が合い、キュー投入のタイミング計算に手を入れずに
  済むから。間を入れるのはフィラーだけ (ニュースと INTRO は後ろに音楽か次の
  発話が来る)

### 落とし穴: 先読みを一時停止で止めてはいけない

`pump_loop` の `paused` 判定を `_ensure_prefetch()` より**前**に置くと、一時停止中に
先読みタスクが 1 本も作られない。`start()` も先読みを蒔かないので (pump_loop が
動き出すのは `start()` が終わってから)、PLAY した瞬間にゼロから原稿生成が始まる。

実測: 起動直後に 90 秒 PAUSE してから PLAY すると、INTRO (32 秒) では生成が
終わらず、**そのあと 45 秒ぶん相槌が続いた**。相槌は 1 本 1 秒程度なので 19 本
連続で鳴り、聴感上は「ずっとフィラーを喋っている」状態になる。

**先読みは再生と独立なので、一時停止で止める理由がない。** 判定の前に出し、
`start()` でも蒔く。後者は一時停止と関係なく、通常の起動も速くなる。

---

## 4. Liquidsoap 連携と track_event

スピーチ → フィラー → 音楽が**逐次 1 本のキュー**なので、news_service が
「今どのファイルが再生中で、いつ終わるか」を知らないと、プール確認と
フィラー投入の判断が遅れて**無音 (dead air) になる**。

### 採用方式: Liquidsoap からの HTTP コールバック

`radio.liq` の `on_track` フックから、トラック切り替わりのたびに POST する。

```
POST http://localhost:8765/api/track_event
{ "event": "track_start", "filename": "cache/scripts_tts/news_0012.wav" }
```

- news_service はファイル名で「いま何が始まったか」を識別して状態を進める
- 各 wav の長さは合成時に既知なので、`track_start` + 長さで終了予測が立つ。
  **終了の `[program] prefetch_lead_sec` 秒前に次の投入判断**を行い、
  キューを絶やさない
- telnet ポーリングは補助・デバッグ用 (`queue_state()`)
- 表示イベント (背景切替・字幕・リップシンク開始) もこの track_event を起点に
  news_service が WebSocket で配信する。**実再生と表示の同期はこの一本に集約**

### 落とし穴 1: `flush_and_skip` を空のキューに投げてはいけない

`request.queue` には flush 系がこれしかないが、何も鳴っていないときに呼ぶと
skip が「予約」され、**次に push した 1 本目が即座に飛ばされる**。
起動直後の INTRO が 1 秒で切れて発覚した。
`LiquidsoapClient.flush_if_busy()` で回避している。

### 落とし穴 2: Liquidsoap の `json.stringify` は連想リストを JSON 配列にする

`[["event","track_start"],…]` が飛んでいた。オブジェクトが欲しいので
`radio.liq` 側で文字列を組み立て、エスケープだけ `stringify` に任せている。

### 落とし穴 3: telnet のポーリングで Liquidsoap のログが膨れる

毎秒 2 コマンド叩いていて 25 分で 7000 行。`settings.server.log.level` を 2 に
下げ、2 コマンドを 1 接続にまとめた (`queue_state()`)。

### 音声の正規化 (必須)

VOICEVOX 音声と HeartMuLa 楽曲はラウドネスが大きく異なる。
**全 wav をキュー投入前に ffmpeg loudnorm (EBU R128) で正規化**してから
push する。サンプルレートも投入前に統一する (`[loudness] sample_rate`)。

Liquidsoap 側の normalize でも同じことはできるが、ファイル段階で揃える方が
試聴・デバッグしやすいのでこちらを採った。

### 表示系 (web/)

three-vrm + リップシンク、字幕、WebSocket、操作ボタンは AIjukebox から流用。
AIradio で加えたのは以下。

- **背景**: いま読んでいるニュースのスクリーンショット (png) を表示する。
  MUSIC_PLAYING 中は**直前のニュースのまま残す** (曲はニュースと無関係なので、
  ここで背景を消すと画面が意味を失う)
- **字幕**: ニュース原稿 / フィラーのテキスト
- **出典表示**: `Source: GIGAZINE` と記事タイトルを**常時表示**する。
  要約を読み上げる番組なので、出典が画面から消える状態を作らない
- **クレジット**: VOICEVOX 話者クレジットは AIjukebox 同様に表示
- 操作ボタンは PAUSE/RESUME と SKIP (次のニュースへ) のみ

表示の駆動は上記 track_event 一本に集約されている。リップシンクのズレは
`[program] lipsync_delay_ms` で補正する (wav を push してから実際に音が出るまでの
遅れぶん)。

---

## 5. ニュース選定

- 対象は `[news] days` 日分の `news/YYYY-MM-DD/` 配下の記事
- 直近に読んだ `[news] exclude_ring` 件の id を除外リング (deque) で弾き、
  残りから一様ランダム
- 除外リングは `db/state.json` に**永続化**する (再起動で同じニュースを
  連続で読まないため)

### 記事数は除外リングより多くないと効かない

記事が 3 件しかなかったとき、除外リング (10 件) には同じ id が並び、
310 サイクルで 3 記事を 96〜107 回ずつ読んでいた。害はないが機能していない。

記事を 13 件に増やして 143 サイクル回したところ、**13 記事すべてが 9〜12 回に
均等に散り、同じ記事の再登場は最短でも 11 サイクル** (平均 13.0) 空いた。
リング長 10 + ランダム選択の設計どおり。

**`[news] exclude_ring` より十分多い記事を切らさないこと**が前提条件で、
そのためには Gcrawler を毎日回す必要がある (`--limit 10` 以上)。

---

## 6. BGM プールと収支

ここが本プロジェクトで一番きわどい部分。

### 6.1 Phase 0 ベンチ — 「30 秒生成 ≒ 60 秒」は成り立たない

HeartMuLa の「30 秒の曲を約 60 秒で生成」は**単独実行時**の値。実運用では
同じ GPU 上で llama-server (Qwen3.6-35B-A3B) が原稿を作り続ける。
その状態で実測した (2026-08-10 / gfx1151 / 生データ `logs/bench_phase0.json`)。

| 条件 | 所要 (中央値) | 生成された音 (中央値) | 音1秒あたり |
|---|---|---|---|
| 単独 | 64.9 秒 | 22.7 秒 | 5.36 秒 |
| llama-server 同時 | 141.6 秒 | 30.1 秒 | **4.71 秒** |

同時に回した原稿生成は 74 回・失敗 0 回・中央値 2.62 秒。
**LLM 側は BGM 生成と同居しても実用速度を保つ**ので、詰まるのは BGM だけ。
なお同時実行のコストは +28% 程度で、支配的な要因ではない。

### 6.2 指定尺は「上限」でしかない

HeartMuLa は `audio_eos` が出た時点で打ち切る。30 秒を指定しても 7 秒や
22 秒で終わる曲が普通に混ざる。したがって

- 生成時間を「指定尺」で割った比率は**当てにならない**。出来上がった wav の
  長さで割ること
- プール在庫を曲数で数えると、実際に持っている音の秒数は目標より短くなりうる
- 極端な例では **2.3 秒**で終わった。BGM として使えないので、
  `[bgm] min_duration_sec` (既定 18 秒) より短い曲は**捨てて作り直す**。
  生成時間は尺にほぼ比例するので、短い曲を捨てる損は小さい

**破棄率は運転ごとに 29〜37%。** 捨てた曲の平均は 8.0〜9.1 秒で、
`min_duration_sec` を 18 → 15 秒に下げてもほとんど拾えない。
**HeartMuLa は 3 回に 1 回は曲にならない**、と考えて設計するのが正しい。

### 6.3 プロセス最初の 1 曲は 2〜4 倍遅い

MIOpen のカーネル探索が入るため、1 本目だけ 250 秒かかる (2 本目以降は 141 秒)。
`bgm_worker` を常駐させる設計 (毎回モデルをロードし直さない) はこの点でも正しい。
**起動直後だけプールの回復が遅いのは仕様**と考えてよい。

### 6.4 生成時間はほぼ尺に比例する

同時負荷時の 2 点 (7.0 秒 → 53.6 秒 / 30.1 秒 → 141.6 秒) を直線で結ぶと

```
生成時間 ≒ 27 秒 + 3.8 × 音の秒数
```

固定コスト 27 秒に対して比例項の係数が 3.8 と大きい。これが次の
「尺を伸ばす緩和策が効かない」理由になる。

### 6.5 緩和策の評価

1 サイクル = スピーチ (最大 35 秒) + BGM (30 秒) = 65 秒。このあいだに BGM を
1 曲消費するのに、1 曲の生成には 141 秒かかる。**供給が消費の 0.46 倍**しかない。

| 案 | 評価 |
|---|---|
| `pool_target` を増やして空き時間に貯金する | **効かない**。24 時間連続運転なので貯金する原資 (アイドル時間) が存在しない。在庫を厚くしても枯れるまでの時間が延びるだけ |
| BGM の尺を 45〜60 秒に伸ばして消費レートを下げる | **逆効果**。生成時間が尺にほぼ比例する (係数 3.8) ため、尺を倍にすると生成時間もほぼ倍になる。収支式 `27 + 3.8D ≦ 35 + D` を解くと `D ≦ 2.8 秒` で、伸ばす方向に解は無い |
| 原稿生成と BGM 生成の時間帯をずらす | **効かない**。同上、ずらす先の時間帯が無い |
| **同じ曲を複数サイクル使い回す** | **採用** (下記) |

**採用: `[bgm] reuse_count`。** プールから取り出した曲を捨てずに、指定回数ぶん
繰り返し流してから `used/` へ退ける。消費レートが `1/reuse_count` になる。

```
必要な回数 = 1曲の生成時間 / 1サイクル = 141 / 65 = 2.2 → 3 回以上
```

BGM はニュースと連動しない独立生成 (§2-2) なので、同じ曲が数サイクルおきに
再登場しても番組の筋は壊れない。実装は最古の 1 曲を取り出して mtime を
更新するだけなので、プール内を順繰りに回る (**同じ曲が連続して 2 回鳴ることはない**)。

再生回数は `db/state.json` の `bgm_plays` に持つ。プールから消えた曲の
カウントは次回 `take_bgm` 時に落とされる。

### 6.6 `used/` は保険なので消しすぎない・溜めすぎない

退けた曲は消さずに `cache/bgm_pool/used/` へ移す。**プールが枯れたときに手で
戻して延命するため**で、実際に使う場面がある (下記)。

ただし 1 曲 5 MB あるので上限が要る。`reuse_count = 5` / 1 サイクル 60 秒だと
**3 日で約 850 曲 = 4.2 GB** のペースで増える。`[bgm] used_keep` (既定 100 曲) で
mtime の新しい順に残し、`take_bgm` が退避したその場で削る。

**起動時の掃除に任せてはいけない。** `start_all.sh` も 3 日より古いものを消すが、
あれは起動時にしか走らないので連続運転では効かない。

退避した曲を返す前に `os.utime(dst)` している。`shutil.move` は mtime を保つので、
これが無いと `reuse_count = 1` のとき (一度も utime されないまま) 直後の
`trim_used` に消され、呼び出し側が存在しないパスを受け取る。

#### プールが枯れたときの戻し方

`used/` から条件に合うものをプールへコピーする。指紋 (`prompt_hash`) が現在の
設定と一致し、`min_duration_sec` を満たすものだけにすること。`shutil.copy2` で
mtime を保てば `take_bgm` の順繰りにそのまま乗る。

実例: デバッグで短時間に 4 回再起動したらプールが枯れた。`bgm_worker` は
プロセスの 1 本目に 270 秒かかる (§6.3) ので、再起動のたびにこれを払い直して
いた。`used/` から 10 曲戻して即座に FILLER_LOOP を抜けた。

### 6.7 使わなかったが有効な選択肢

- **原稿を長くする**: 収支式は `27 + 3.8D ≦ S + D`。D=30 のままで成立させるには
  スピーチ S ≧ 111 秒 (約 800 字) が要る。「30 秒のニュース」という番組の
  かたちを変えることになるので採らなかった。長尺ニュース番組にしたいなら
  `[script] max_chars` と `max_wav_sec` を上げれば成立する
- **BGM を短くする**: 収支だけなら `duration_sec = 9` 以下で成立するが、
  それは BGM ではなくジングルになる

### 6.8 収支は「総時間 ÷ 採用曲数」で測ってはいけない

**これは一度間違えた。**

`bgm_worker` は在庫が `pool_target` に達すると生成を止める。したがって
「運転時間 ÷ 採用曲数」は**需要で頭打ちになった値**であって、生成能力ではない。
在庫が安定していれば、この値は必ず消費レートの近くに張り付く。

2 時間 25 分の運転では、消費が 302 秒に 1 曲、採用が 298 秒に 1 曲で
「余裕 1%」に見えた。実際には **worker は 49% の時間を待機していた**。

**能力は「生成していた時間 ÷ 採用曲数」で測る。**

| 運転 | 生成中の時間 | 採用 | 能力 |
|---|---|---|---|
| 2026-08-11 (2h25m) | 4421 秒 | 29 曲 | **152 秒に 1 曲** |
| 2026-08-10 (5h12m) | 約 13500 秒 | 82 曲 | **165 秒に 1 曲** (概算) |

`reuse_count = 5` のときの消費は 5 × 約 60 秒 = **約 300 秒に 1 曲**。
**能力はその約 2 倍**あり、余裕は十分。以前の記録にあった「余裕 5%」
「余裕 24%」はどちらもこの測り方の誤りによる過小評価だった。

---

## 7. 原稿生成

- 入力は選定した記事の `title` + `summary`
- 出力は**約 30 秒で読める日本語原稿**。`[script] max_chars` (既定 220 字) で
  上限を指示し、さらに**合成後の wav 長で実測確認**する。
  `[script] max_wav_sec` (35 秒) を超えたら `max_regen` 回まで作り直す
- 口調はラジオ DJ 調。`[dj] persona` と `[voicevox] speaker_name` を
  プロンプトに渡す
- 出典を原稿内で自然に言及する (「GIGAZINE の記事によると」等)
- 直近 `[llm] recent_scripts` 本の原稿をプロンプトに添えて、言い回しの
  重複を避ける。原稿は `logs/program.log` に JSONL で残る

---

## 8. エラー時の振る舞い (番組を止めない)

| 障害 | 振る舞い |
|---|---|
| LLM 原稿生成失敗 | その記事をスキップし次の記事へ。連続失敗時は固定文で場つなぎ |
| INTRO 生成失敗 | 固定フォールバック文を再生 |
| BGM プール枯渇 | FILLER_LOOP (正常系の一部) |
| bgm_worker 死亡 | FILLER_LOOP で回り続ける。ログで気づけるようにする |
| news/ が空 | 固定アナウンス + BGM のみで回す。クローラー実行を促すログを出す |
| VOICEVOX 落ち | 音声が作れないため番組は継続不能 → BGM 連続再生モードに退避 |
| TTS/LLM 一時エラー | 2 回程度リトライ後に上記へフォールバック |

---

## 9. 環境まわりの落とし穴

### torchaudio 2.9 の `save` は torchcodec 必須

gfx1151 向けの torchcodec ホイールが無く、HeartMuLa の postprocess が
ImportError で落ちる。`bgm_gen._patch_torchaudio_save()` で標準ライブラリの
`wave` に差し替えている。

**heartlib 側には手を入れていない。** 上流の取り込みを続けたいので、
環境差の吸収は AIradio 側で閉じる方針。

### `HSA_OVERRIDE_GFX_VERSION` は設定しない

llama.cpp も torch も gfx1151 のネイティブビルドなので、override すると壊れる。
`start_all.sh` は明示的に `unset` している。

### `uv run` ではなく `uv run --no-sync`

`uv sync` が venv を作り直し、ROCm 版 ctranslate2 等を PyPI 版で上書きする。

---

## 10. 実測ログ

いずれも gfx1151 (Radeon 8060S) / Qwen3.6-35B-A3B / VOICEVOX docker 同居。

| | 2026-08-10 | 2026-08-11 (1) | 2026-08-11 (2) |
|---|---|---|---|
| 運転時間 | 5 時間 12 分 | 10 分 43 秒 | 2 時間 25 分 |
| `reuse_count` | 4 | 5 | 5 |
| 記事数 | 3 | 13 | 13 |
| サイクル数 | 310 | 10 | 143 |
| 1 サイクル | 平均 60.3 秒 (45〜65) | 平均 58.7 秒 (47〜64) | 平均 60.8 秒 (49〜65) |
| FILLER_LOOP | 0 | 0 | 0 |
| エラー | 0 | 0 | 0 |
| プール在庫 | 終始 2〜3 | 3 → 2 | 終始 3 |
| BGM 採用 / 破棄 | 82 / 34 (29%) | 2 / 1 | 29 / 17 (37%) |
| 1 曲の生成 (採用分) | 平均 116.4 秒 | — | 平均 122.3 秒 (115.7〜267.4) |
| worker 稼働率 | (未測定) | — | **51%** |

- **`reuse_count` は 4 でも 5 でも番組は破綻しない。** 5 のとき
  `bgm_..._210856.wav` がちょうど 5 回鳴ってから `used/` へ退くことを確認済み
- 生成時間は 2 回目以降 115〜120 秒に安定する。267 秒はプロセス最初の 1 曲 (§6.3)
- ラウドネスは Icecast 実ストリームで **-17.3 LUFS** (目標 -16)

---

## 11. 積み残し

- **Gcrawler の cron が未設定。** 記事が古くなると `[news] days = 3` で
  読める本数が減り、除外リングが効かなくなる (§5)。毎日回すこと
- **長時間の音質を人間が聴いていない。** ラウドネスは測ったが、
  5 時間ぶんを通しで聴いた人はいない
- **HeartMuLa の短尺打ち切り (29〜37%) の原因は未調査。** `tags` / `cfg_scale` /
  `topk` を振ると歩留まりが上がるかもしれない。ただし §6.8 のとおり生成能力に
  約 2 倍の余裕があるので、**優先度は低い**

---

## 12. 再測定のしかた

ハードやモデルを変えたら測り直すこと。判定と必要な使い回し回数まで
ベンチが出す。

```bash
PYTHONPATH=scripts ~/heartlib/.venv/bin/python scripts/bench_phase0.py --clips 3
cat logs/bench_phase0.json    # required_reuse_count をそのまま settings.toml へ
```

運転後に収支を確認するときは、**§6.8 のとおり「生成していた時間 ÷ 採用曲数」**
で能力を出すこと。`bgm_worker` のログの「プール N/M → 生成します」が着手、
「生成:」「短すぎるので破棄:」が完了なので、その差の合計が生成中の時間になる。
